{
  "openapi": "3.0.0",
  "info": {
    "title": "API do Pro-Esporte",
    "description": "API responsável pelo cadastro de usuários e eventos do sistema Pro-Esporte",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://localhost:3333",
      "description": "Servidor local"
    }
  ],
  "paths": {
    "/cadastro": {
      "post": {
        "tags": ["Cadastro"],
        "summary": "Cadastro geral",
        "description": "Cadastra cidadão, empresa ou servidor público",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  { "$ref": "#/components/schemas/CidadaoInput" },
                  { "$ref": "#/components/schemas/EmpresaInput" },
                  { "$ref": "#/components/schemas/ServidorPublicoInput" }
                ]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Cadastro realizado com sucesso" },
          "400": { "description": "Dados inválidos" },
          "409": { "description": "Usuário já cadastrado" }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "CidadaoInput": {
        "type": "object",
        "required": ["tipo", "nome", "email", "senha"],
        "properties": {
          "tipo": { "type": "string", "example": "CIDADAO" },
          "nome": { "type": "string", "example": "João Silva" },
          "email": { "type": "string", "format": "email", "example": "joao@email.com" },
          "senha": { "type": "string", "format": "password", "example": "123456" }
        }
      },
      "EmpresaInput": {
        "type": "object",
        "required": ["tipo", "nome", "email", "CNPJ", "telefone", "senha"],
        "properties": {
          "tipo": { "type": "string", "example": "EMPRESA" },
          "nome": { "type": "string", "example": "Empresa X" },
          "email": { "type": "string", "format": "email", "example": "empresa@email.com" },
          "CNPJ": { "type": "string", "example": "12345678000199" },
          "telefone": { "type": "string", "example": "(31) 99999-9999" },
          "senha": { "type": "string", "format": "password", "example": "123456" }
        }
      },
      "ServidorPublicoInput": {
        "type": "object",
        "required": ["tipo", "CPF", "nome", "email", "telefone", "senha"],
        "properties": {
          "tipo": { "type": "string", "example": "SERVIDOR_PUBLICO" },
          "CPF": { "type": "string", "example": "12345678901" },
          "nome": { "type": "string", "example": "Lucas Epifânio" },
          "email": { "type": "string", "format": "email", "example": "lucas@email.com" },
          "telefone": { "type": "string", "example": "(31) 98888-7777" },
          "senha": { "type": "string", "format": "password", "example": "123456" }
        }
      }
    }
  }
}
