import {
  StyleSheet
} from 'react-native';

import colors
from './StyleColors';

export default StyleSheet.create({
  titulo: {
    color: colors.primary,

    fontSize: 30,

    fontWeight: 'bold',

    marginLeft: 20,

    marginTop: 10,

    marginBottom: 20
  }
});